# WhatsApp session generated by the pairing flow.
# Keep this value private and set it in the host's secret/environment settings.
SESSION_ID=LordBot~YOUR_FRESH_SESSION_ID
PREFIX=.
PORT=3000

# Optional service configuration
GIPHY_API_KEY=